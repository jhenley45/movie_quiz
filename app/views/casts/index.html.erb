<h1>Casts#index</h1>
<p>Find me in app/views/casts/index.html.erb</p>



<ul>
<% @body.first[1].each do | list | %>
	<li>
	<%= list["name"] %>
	</li>
<% end %>
</ul>

<%= form_tag({controller: "movies", action: "index"}, method: "get") do | f |%>
	<%= label_tag(:movie, "Enter an actor/actress:") %>
  <%= text_field_tag(:cast) %>
  <%= submit_tag("Go!") %>
<% end %>
